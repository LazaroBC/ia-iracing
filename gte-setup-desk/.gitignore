# Sistema
.DS_Store
Thumbs.db

# Archivos de setup reales de iRacing (binarios, no se versionan aquí salvo que quieras)
*.sto

# Copias de exportación de setups (.json): se ignoran por defecto, salvo excepciones explícitas
*.json
!cars/**/config.json
!setups-guardados/**/*.json
